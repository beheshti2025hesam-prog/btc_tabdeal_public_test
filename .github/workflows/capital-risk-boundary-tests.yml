name: Capital Risk Boundary Tests

on:
  push:
    branches: ["main", "capital-risk-boundary-v1"]
  pull_request:
    branches: ["main"]

permissions:
  contents: read

jobs:
  capital-risk-boundary:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: python -m pip install --upgrade pip pytest
      - run: pytest -q tests/risk/test_boundary.py
        env:
          PYTHONPATH: .
