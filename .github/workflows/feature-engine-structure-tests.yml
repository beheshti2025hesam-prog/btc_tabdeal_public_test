name: Feature Engine Structure Tests

on:
  push:
    branches: ["main", "feature-engine-structure-v1"]
  pull_request:
    branches: ["main"]

permissions:
  contents: read

jobs:
  feature-engine-structure:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: python -m pip install --upgrade pip pytest
      - run: pytest -q tests/feature_engine/test_structure.py
        env:
          PYTHONPATH: .
