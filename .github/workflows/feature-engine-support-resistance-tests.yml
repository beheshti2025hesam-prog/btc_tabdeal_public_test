name: Feature Engine Support Resistance Tests

on:
  push:
    branches: ["main", "feature-engine-support-resistance-v1"]
  pull_request:
    branches: ["main"]

permissions:
  contents: read

jobs:
  feature-engine-support-resistance:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: python -m pip install --upgrade pip pytest
      - run: pytest -q tests/feature_engine/test_support_resistance.py
        env:
          PYTHONPATH: .
